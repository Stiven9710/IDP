# ========================================
# CONFIGURACIÓN DEL SISTEMA IDP EXPERT
# ========================================

# Azure OpenAI Configuration
AZURE_OPENAI_ENDPOINT=https://uc-openai.openai.azure.com/
AZURE_OPENAI_API_KEY=tu-api-key-aqui
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4o
AZURE_OPENAI_API_VERSION=2025-01-01-preview

# Azure Document Intelligence Configuration
AZURE_DOCUMENT_INTELLIGENCE_ENDPOINT=https://uc-ocr.cognitiveservices.azure.com/
AZURE_DOCUMENT_INTELLIGENCE_API_KEY=tu-api-key-aqui

# Azure Cosmos DB Configuration
AZURE_COSMOS_ENDPOINT=https://uc-dbcosmos-nosql.documents.azure.com:443/
AZURE_COSMOS_KEY=tu-api-key-aqui
AZURE_COSMOS_DATABASE_NAME=idp-database
AZURE_COSMOS_CONTAINER_DOCUMENTS=documents
AZURE_COSMOS_CONTAINER_EXTRACTIONS=extractions
AZURE_COSMOS_CONTAINER_JOBS=processing_jobs

# Azure Storage Configuration (Blob + Queue)
# Opción 1: Connection String (Recomendado)
AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=idpstorage;AccountKey=tu-account-key;EndpointSuffix=core.windows.net

# Opción 2: Account Name + Key (Alternativa)
AZURE_STORAGE_ACCOUNT_NAME=idpstorage
AZURE_STORAGE_ACCOUNT_KEY=tu-account-key-aqui

# System Configuration
SYNC_PROCESSING_THRESHOLD_MB=10
MAX_PROCESSING_TIME_SECONDS=300
MAX_FILE_SIZE_MB=50
LOG_LEVEL=INFO

# CORS Configuration
CORS_ORIGINS=["*"]

# ========================================
# INSTRUCCIONES DE CONFIGURACIÓN
# ========================================

# 1. Copiar este archivo a .env
#    cp env.example .env

# 2. Editar .env con tus credenciales reales

# 3. Para Azure Storage, puedes usar:
#    - Connection String (más simple)
#    - Account Name + Key (más flexible)

# 4. Verificar que todos los servicios estén funcionando:
#    - GET /api/v1/health
#    - GET /api/v1/health/cosmos
#    - GET /api/v1/health/blob-storage
#    - GET /api/v1/health/queue-storage
#    - GET /api/v1/health/storage

# ========================================
# NOTAS IMPORTANTES
# ========================================

# - Nunca subir .env a Git (ya está en .gitignore)
# - Usar credenciales de desarrollo para pruebas locales
# - Usar credenciales de producción solo en servidores de producción
# - Rotar API keys regularmente por seguridad
